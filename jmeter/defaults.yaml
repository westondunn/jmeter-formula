# -*- coding: utf-8 -*-
# vim: ft=yaml
---
jmeter:
  pkg:
    name: jmeter
  rootgroup: root
  config: '/etc/jmeter'
  service:
    name: jmeter
  subcomponent:
    config: '/etc/jmeter-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
